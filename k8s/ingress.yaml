apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: interview-api-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: interview-api.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: interview-api-svc
                port:
                  number: 80
