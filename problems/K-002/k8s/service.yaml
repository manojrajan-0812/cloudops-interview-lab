apiVersion: v1
kind: Service
metadata:
  name: interview-api-svc
  namespace: default
spec:
  # BUG: selector doesn't match deployment label (app: interview-api)
  selector:
    app: api-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
