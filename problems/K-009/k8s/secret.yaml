apiVersion: v1
kind: Secret
metadata:
  name: interview-api-secret
  namespace: default
type: Opaque
data:
  # BUG: API_KEY was moved to ConfigMap — it's missing here
  # PLACEHOLDER: add API_KEY here (base64 encoded value)
