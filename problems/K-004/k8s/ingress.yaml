apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: interview-api-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: interview-api.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: interview-api-svc
                # BUG: should be 80 (the Service port), not 8080 (the container port)
                port:
                  number: 8080
